# MongoDB connection string for Railway
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/campus-pc-monitoring?retryWrites=true&w=majority

# JWT secret for admin authentication
JWT_SECRET=your_jwt_secret_here

# Optional CORS origin for frontend deployment
CORS_ORIGIN=https://your-frontend-url.vercel.app
